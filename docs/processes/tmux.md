# Tmux

- Instalación

    `$ sudo apt install tmux`
    
- Crear nueva sesión

    `$ tmux new -s <nombre>`

- Ingresar a una sesión

    `$ tmux attach -t <nombre>`

- Salir de una sesión sin terminarla

    `$ Ctrl + B, D`